<h1>All Students</h1>

<% @students.each do |student| %>
  <%= student.name %>
  <%= link_to 'Delete', student_path(student), method: :delete %>
<% end %>

<p><%= link_to "Create New", new_student_path %></p>
